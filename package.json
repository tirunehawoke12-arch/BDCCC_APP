{
  "name": "bdccc-backend",
  "version": "1.0.0",
  "description": "Real backend (key-value API) for the Bahir Dar Childcare Center app — lets all devices share the same live data instead of each browser storing its own copy.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
